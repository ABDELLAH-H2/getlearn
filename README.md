# Git Learning Project

This is a practice project for learning Git and GitHub commands.

## How to use this project

1.  Open your terminal in this directory.
2.  Run `git status` to see these new files.
3.  Add them with `git add .`
4.  Commit them with `git commit -m "Initial commit"`